mutation UpdateProductAttributes($id: ID!, $attributes: [AttributeValueInput!]) {
  productUpdate(id: $id, input: { attributes: $attributes }) {
    errors {
      field
      message
      code
    }
    product {
      id
    }
  }
}
